# longestcommonsubsequence Problem
- Problem: I want you to find the longest common subsequence between two given strings. A common subsequence is a 
string that contains elements common to two or more strings. This common subsequence must appear sequentially in
the input arrays, but not necessarily consecutively.